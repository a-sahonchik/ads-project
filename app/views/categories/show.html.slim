- breadcrumb @category
h1
  = t("attributes.category.id.#{@category.id}")
= will_paginate @advertisements, renderer: 'BootstrapPaginationHelper::LinkRenderer'
= render '/advertisements/advertisements'
= will_paginate @advertisements, renderer: 'BootstrapPaginationHelper::LinkRenderer'
