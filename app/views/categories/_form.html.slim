= simple_form_for @category, defaults: { input_html: { class: 'form-control' } } do |f|
  = f.input :name
  = f.button :submit, class: 'btn btn-primary'
  = link_to t('common.back'), categories_path, class: 'btn btn-outline-secondary'
